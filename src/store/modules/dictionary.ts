const dictionary = {
  namespace: true,
  state: {
    dict: {}
  },
  mutations: {
    // SET_DICTIONARY: (state, code) => {
    //   state.dict = code
    // }
  },
  actions: {
    // getDictionary({ commit }, code) {
    //   commit('SET_DICTIONARY', code)
    // }
  }
};

export default dictionary;
